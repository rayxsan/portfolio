import styled from "styled-components";

export const StyledDropdown = styled.div`
  label,
  footer {
    font-family: sans-serif;
  }

  label {
    font-size: 1rem;
    padding-right: 10px;
  }

  select {
    font-size: 0.9rem;
    padding: 2px 5px;
  }
`;
